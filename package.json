{
  "name": "sqtracker",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "api:dev": "yarn workspace @sqtracker/api dev",
    "client:dev": "yarn workspace @sqtracker/client dev",
    "dev": "npm-run-all --parallel api:dev client:dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
